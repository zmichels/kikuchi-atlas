[pytest]
testpaths = tests
markers =
    gpu: requires a working WebGPU/Metal adapter
    slow: explicit long-running simulation, full-resolution relief, or rendering evidence
    mtex: requires MATLAB and MTEX 6.1.1
